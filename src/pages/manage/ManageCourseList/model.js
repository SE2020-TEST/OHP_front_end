import { addFakeList, queryFakeList, removeFakeList, updateFakeList } from './service';

const Model = {
  namespace: 'manageCourseList',
  state: {
    
  },
  effects: {
    
  },
  reducers: {
    
  },
};
export default Model;
